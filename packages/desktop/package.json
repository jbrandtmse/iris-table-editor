{
  "name": "@iris-te/desktop",
  "version": "0.1.0",
  "description": "Desktop application components for IRIS Table Editor (Electron shell, connection management, UI)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "compile": "tsc",
    "watch": "tsc --watch",
    "lint": "eslint src",
    "check-types": "tsc --noEmit",
    "test": "node --test dist/test/**/*.test.js",
    "start": "electron dist/main/main.js",
    "generate-icons": "node scripts/generate-icons.js",
    "stage-assets": "node scripts/stage-assets.js",
    "dist": "npm run compile && npm run stage-assets && electron-builder",
    "dist:win": "npm run compile && npm run stage-assets && electron-builder --win",
    "dist:mac": "npm run compile && npm run stage-assets && electron-builder --mac",
    "pack": "npm run compile && npm run stage-assets && electron-builder --dir"
  },
  "files": [
    "dist",
    "src/ui"
  ],
  "dependencies": {
    "@iris-te/core": "*",
    "electron-updater": "^6.3.0"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-builder": "^25.1.0"
  },
  "license": "MIT"
}
